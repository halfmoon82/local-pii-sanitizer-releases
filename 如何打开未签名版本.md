# 如何打开未签名版本（种子用户单页说明）

你拿到的是 **本地脱敏 Agent 企业版** 的种子版本安装包（`LocalPIISanitizer-<版本>-<芯片>.dmg`）。在正式签名版发布之前，它没有经过 Apple 的公证，所以第一次打开时 macOS 会拦一下。这是系统对所有未公证软件的统一提醒，不代表软件有问题；按下面四步做一次即可，之后双击就能直接打开。

## 安装

1. 双击 dmg，把 **本地脱敏 Agent 企业版** 图标拖到旁边的「应用程序」文件夹。
2. 在「应用程序」里找到 **LocalPIISanitizer**，双击。

## 第一次打开被拦住时（每个新版本一次）

看到 **「无法打开 "LocalPIISanitizer"，因为 Apple 无法检查其是否包含恶意软件」** 或 **「无法验证开发者」**：

1. 点 **「完成」**（或「好」）。**不要**点「移到废纸篓」。
2. 打开  → **系统设置** → **隐私与安全性**。
3. 向下滚动到「安全性」一栏，会看到一行「已阻止使用 "LocalPIISanitizer"，因为它不是来自已确认的开发者」。点它右边的 **「仍要打开」**。
4. 再点一次 **「仍要打开」** → 输入开机密码（或触控 ID）→ 应用启动。

不需要关闭系统的任何安全设置。熟悉终端的用户也可以用一条命令代替上面四步（效果相同，去掉系统给下载文件打的隔离标记）：

```bash
xattr -dr com.apple.quarantine /Applications/LocalPIISanitizer.app
```

## 打开之后

- 首次启动会出现「首次准备」页面：点「开始下载」，一次性下载约 3.4 GB 的本地识别模型（只保存在本机，不会上传任何文件）。下载完成后脱敏 / 还原按钮才可用。
- 你的数据、加密映射表与设置都保存在应用之外（`~/Library/Application Support/LocalPIISanitizer/` 与 `文稿/本地脱敏/`），更新版本时不受影响；其中 `vault` 文件夹是还原文件的唯一钥匙，请勿删除或分享。
- 详细说明（系统要求、从旧版导入、端口占用、激活与续费、卸载、常见问题）见随包的《安装与使用说明》（INSTALL.md）。

## 系统要求

macOS 14 (Sonoma) 及以上；Apple Silicon 请用文件名带 `arm64` 的包，Intel 机器用带 `x86_64` 的包（ → 关于本机 可以看到芯片类型）。磁盘约 5 GB，内存建议 16 GB。

## 常见疑问

**为什么会有这个提示？** 因为种子版本还没有经过 Apple 的开发者签名与公证。正式版会带签名，届时这一步消失。

**「仍要打开」找不到？** 这一行只在你刚刚双击过应用、被拦住之后的几分钟内出现。回到「应用程序」再双击一次应用，然后马上回到 系统设置 → 隐私与安全性 查看。

**macOS 15 / 26 里右键「打开」不管用了？** 是的，从 macOS 15 起 Apple 取消了右键打开的绕过方式，只能走上面的「系统设置 → 隐私与安全性 → 仍要打开」。

**更新到新版本后又被拦了？** 正常，每个新版本都要做一次上面的四步。
