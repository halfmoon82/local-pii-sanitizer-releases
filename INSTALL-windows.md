# 本地脱敏 Agent 企业版 — 安装与使用说明（Windows）

> 本地脱敏 Agent 是一个**全程在你自己电脑上运行**的文档脱敏工具：把 Word / Excel / PPT / PDF / 扫描件里的姓名、手机号、身份证号、公司名等敏感信息自动替换成占位符，你只把脱敏后的版本交给云端 AI；AI 处理完的结果放回来，本机自动把占位符还原成原文并重新生成 Word。原始敏感信息只以加密形式保存在你的电脑上，云端永远看不到。

macOS 版说明见 [INSTALL.md](INSTALL.md)。两个平台的功能一致，只有第 10 节列出的两处差异。

## 1. 系统要求

| 项 | 要求 |
|---|---|
| 系统 | Windows 10（1809 及以上）或 Windows 11，64 位 |
| 处理器 | x64（Intel / AMD）。ARM 版 Windows 未做验证，不建议使用 |
| 磁盘 | 约 5 GB 可用空间（应用约 1 GB + 首次准备下载的本地大模型约 3.4 GB） |
| 内存 | 建议 16 GB（本地大模型运行时常驻约 4 GB） |
| 其它组件 | 需要 Microsoft Edge WebView2 运行时。Windows 11 与近两年更新过的 Windows 10 都已自带；缺失时界面会排版错乱，按第 11 节安装即可 |
| 网络 | **只在两个时刻需要联网**：首次准备下载本地大模型（一次性）、激活 / 续费校验。日常脱敏与还原完全离线，不上传任何文档内容 |

不需要安装 Python 或任何其它软件——所需组件已全部打包在安装包内。

## 2. 安装

1. 双击下载的 `LocalPIISanitizer-<版本>-win-x64-Setup.exe`。
2. 如果出现蓝底的「Windows 已保护你的电脑」提示，按第 3 节处理。
3. 按向导下一步即可。安装位置默认是 `%LOCALAPPDATA%\Programs\LocalPIISanitizer`，**不需要管理员权限**，也不会碰系统目录。
4. 安装完成后，在开始菜单里搜「本地脱敏」就能找到它（也可以在安装最后一步勾选创建桌面快捷方式）。

更新到新版本时直接运行新的安装包，覆盖安装即可；你的数据、设置、激活状态都在安装目录之外（见第 6 节），不受影响。

## 3. 提示「Windows 已保护你的电脑」怎么办（未签名的种子版本）

在购入代码签名证书之前，你拿到的安装包没有数字签名。首次运行时 Microsoft Defender SmartScreen 会弹出蓝底提示 **「Windows 已保护你的电脑 / Microsoft Defender SmartScreen 阻止了无法识别的应用启动」**。这是系统对所有未签名程序的统一提醒，不代表安装包有问题。处理方法：

1. 点提示框里的 **「更多信息」**（这一行字很不显眼，在提示文字下方）。
2. 展开后会显示应用名称和发布者，点右下角新出现的 **「仍要运行」** 按钮。
3. 安装继续，之后不再提示。

如果你的公司用 360 安全卫士、电脑管家一类的安全软件，安装时也可能被拦下并提示「未知程序」，选择「允许运行」或「信任此程序」即可。若安全软件直接把文件删掉了，请从隔离区恢复并加入白名单，或联系我们。

想在安装前自己核对文件完整性：安装包旁边有同名的 `.sha256` 文件，在 PowerShell 里运行

```
Get-FileHash .\LocalPIISanitizer-<版本>-win-x64-Setup.exe -Algorithm SHA256
```

把输出的哈希值与 `.sha256` 文件里的对照，一致即文件未被改动。

## 4. 首次准备（一次性，下载本地大模型）

识别人名和公司名需要一个在本机运行的大模型（约 3.4 GB）。第一次启动时应用会打开「首次准备」页面：

1. 点「开始下载」，页面上有进度条；下载期间可以最小化窗口，中途关闭应用下次会**断点续传**。
2. 下载完成后页面自动消失，脱敏与还原按钮变为可用。完成前这两个按钮是灰色的。
3. 下载全程只是把模型文件保存到本机，**不会上传你的任何文件**。

**网络不畅（下载很慢或反复失败）时的替代办法**：在另一台网络通畅的电脑上安装同一版本并完成首次准备，然后把那台电脑数据目录下的 `models` 文件夹整个复制到本机的数据目录（数据目录位置见第 6 节），再回到页面点「重试」，应用会直接识别到已有模型。

## 5. 从 macOS 版迁移

加密映射表可以跨平台使用：把 macOS 数据目录 `~/Library/Application Support/LocalPIISanitizer/` 下的 `vault` 文件夹复制到 Windows 的数据目录（见第 6 节）即可，旧批次仍能还原。**激活码是按电脑绑定的**，换机需联系我们换绑，不要直接复制 `license` 文件夹。

## 6. 你的数据在哪里

| 内容 | 位置 |
|---|---|
| 脱敏后的文件（发给云端 AI 的版本） | `文档\本地脱敏\脱敏文件\`（可在界面顶部改） |
| 还原后的 Word | `文档\本地脱敏\还原文件\`（可在界面顶部改） |
| 加密映射表与密钥 `vault\`、日志 `logs\`、授权凭证 `license\`、本地模型 `models\`、原始文件与云端返回的暂存 | `%LOCALAPPDATA%\LocalPIISanitizer\`（在文件资源管理器地址栏粘贴 `%LOCALAPPDATA%\LocalPIISanitizer` 可直达） |

**`vault` 文件夹绝不要同步到网盘、发给别人或删除**：里面是能把占位符换回原文的唯一钥匙，丢了就无法还原已脱敏的文件（这是有意的安全设计，我们也无法帮你找回）。建议定期把整个数据目录备份到移动硬盘。

安装目录只读不写，所有运行产生的内容都在上面两个位置。

## 7. 启动时提示「端口 8814 已被占用」

应用在本机的 8814 端口提供界面。如果电脑上有别的程序正占用这个端口，应用会明确报错并退出，**不会**偷偷换一个端口。处理：

- 最常见的原因是上一次的应用没有完全退出：打开任务管理器，结束还在运行的 `LocalPIISanitizer.exe`，再重新打开。
- 想看是谁占用了端口，在 PowerShell 里运行 `Get-NetTCPConnection -LocalPort 8814`。
- 确实需要换端口时，用命令行启动并指定端口，例如：
  `"%LOCALAPPDATA%\Programs\LocalPIISanitizer\LocalPIISanitizer.exe" --port 8824`

## 8. 激活与续费

- 企业版可免费试用 30 次（脱敏 / 还原共用一个额度）。用完后页面**顶栏出现激活码输入框**，粘贴激活码点「激活」即可（此时需要联网，超时 8 秒）。
- 购买：打开 <https://lic.socialmore.cn/buy>，微信扫码付款后**页面直接显示激活码**（请立即保存）；丢失可到 <https://lic.socialmore.cn/recover> 凭微信支付订单号找回。
- 一个激活码绑定一台电脑；换电脑请联系客服换绑，之后在新电脑用原激活码重新激活。
- 若 `lic.socialmore.cn` 打不开，可改用 <https://lic.socialmore.net>（同一后台）：在数据目录（第 6 节）新建或编辑 `config.local.yaml`，写入一行 `license: {server_url: "https://lic.socialmore.net"}` 后重启应用。
- 激活后每次启动会静默校验一次（续费 / 换绑后自动生效）；校验失败不影响使用，沿用本机凭证，断网也照常可用。

## 9. 卸载

1. 退出应用（关闭窗口）。
2. 在 设置 → 应用 → 已安装的应用 里找到 **本地脱敏 Agent 企业版**，点卸载（也可以在开始菜单的程序组里点「卸载」）。
3. 卸载器**不会**删除你的数据目录 `%LOCALAPPDATA%\LocalPIISanitizer\`，卸载结束时会弹一个提示告诉你数据留在哪里。如果以后还可能需要还原旧的脱敏文件，请保留它（至少保留其中的 `vault`）；确认不再需要时再手动删除该文件夹（其中 `models` 约 3.4 GB）。

## 10. 与 macOS 版的两处差异

**只输出 Word，不输出 PDF。** Windows 版首版不随包 PDF 排版引擎（它需要额外一套 GTK3 图形库，会让安装包再大一截）。界面里 PDF 选项是禁用状态，还原一律输出 docx。需要 PDF 时，用 Word 打开还原结果另存为 PDF 即可，排版与直接生成的一致。

**本地模型只用 CPU 推理。** 随包的 Ollama 已裁去 CUDA 显卡加速库（那部分单独就有 1.8 GB）。识别速度取决于 CPU：一份一万字左右的文档大约两到三分钟。如果你有独立显卡并希望用上它，可以自行安装官方版 Ollama，我们会在后续版本提供接入选项。

## 11. 常见问题

**双击没反应 / 窗口一闪就没了**
　先确认系统是 64 位的 Windows 10 1809 及以上。若仍不行，在浏览器里打开 <http://127.0.0.1:8814>：如果界面能正常显示，说明程序本体在跑、只是应用窗口起不来，可以先在浏览器里正常使用，同时把情况告诉我们。

**窗口打开了，但界面排版错乱、按钮点不动**
　这是缺 Microsoft Edge WebView2 运行时的典型表现：缺失时窗口会退回到早已停止维护的 IE 内核渲染，本界面不支持它。请安装 WebView2 运行时（微软官网搜 "WebView2 Runtime"，下载 Evergreen 独立安装包），安装后重开应用即可。在浏览器里打开上面那个地址是同样可用的临时替代。

**界面右上角「健康」有红项**
　点开看是哪一项：
　- 「ollama」红：本地模型服务还没起来，等 10 秒刷新；若一直红，退出应用后重新打开。
　- 「模型」红：首次准备没有完成，回到首次准备页面继续下载。
　- 其它项红：属于安装包问题，请把健康页截图发给我们。

**杀毒软件报毒**
　安装包未签名且体积较大（内含 Python 运行时与本地模型服务），部分国产安全软件会给出「未知程序」的启发式告警。可把安装目录加入白名单，或把误报样本提交给对应厂商。我们不使用任何加壳压缩（这正是为了降低误报），文件哈希公开可校验（见第 3 节）。

**退出后本地模型还在运行吗？**
　不会。关闭窗口退出应用时，应用会一并停止它拉起的本地模型服务；任务管理器里不会残留 `ollama.exe`。应用使用的是自带的私有模型服务（端口 11555），不会碰你自己另外安装的 Ollama。

**脱敏结果里还是漏了某个名字**
　本地小模型识别并非 100%，界面提供的「审计」会列出疑似残留供人工复核；也可以在设置里把固定要脱敏的词加入词典。任何脱敏产物在发给云端 AI 前请自己再过一眼。

**想在浏览器里而不是应用窗口里用**
　应用运行时，用任意浏览器打开 <http://127.0.0.1:8814> 就是同一个界面（应用窗口不要关，关了服务就停了）。

---

隐私承诺：本软件运行期不上传任何文档内容；唯一的联网请求是激活 / 续费校验，请求体只有 激活码、设备指纹哈希、软件版本 三个字段。完整条款见随包的《最终用户许可协议》（LICENSE.txt），第三方组件声明见 THIRD_PARTY_LICENSES.md。
