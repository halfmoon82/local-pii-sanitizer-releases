# 本地脱敏 Agent 企业版 — 安装与使用说明（macOS）

> 本地脱敏 Agent 是一个**全程在你自己电脑上运行**的文档脱敏工具：把 Word / Excel / PPT / PDF / 扫描件里的姓名、手机号、身份证号、公司名等敏感信息自动替换成占位符，你只把脱敏后的版本交给云端 AI；AI 处理完的结果放回来，本机自动把占位符还原成原文并重新生成 Word / PDF。原始敏感信息只以加密形式保存在你的电脑上，云端永远看不到。

## 1. 系统要求

| 项 | 要求 |
|---|---|
| 系统 | macOS 14 (Sonoma) 及以上 |
| 芯片 | Apple Silicon（M 系列）或 Intel 均可，请下载对应芯片的安装包（文件名末尾 `arm64` = Apple Silicon，`x86_64` = Intel；不确定时点左上角  → 关于本机 查看） |
| 磁盘 | 约 5 GB 可用空间（应用约 1 GB + 首次准备下载的本地大模型约 3.4 GB） |
| 内存 | 建议 16 GB（本地大模型运行时常驻约 4 GB） |
| 网络 | **只在两个时刻需要联网**：首次准备下载本地大模型（一次性）、激活 / 续费校验。日常脱敏与还原完全离线，不上传任何文档内容 |

不需要安装 Homebrew、Python、Xcode 或任何其它软件——所需组件已全部打包在应用内。

## 2. 安装

1. 双击下载的 `LocalPIISanitizer-<版本>-<芯片>.dmg`，会弹出一个窗口。
2. 把窗口里的 **本地脱敏 Agent 企业版** 图标拖到旁边的「应用程序」文件夹。
3. 拖完后在访达顶部菜单 前往 → 应用程序 里找到它，双击打开。此后也可以在启动台里找到它。

更新到新版本时重复这三步，选择「替换」即可；你的数据、设置、激活状态都在应用之外的数据目录里（见第 6 节），不受影响。

## 3. 首次打开时提示「无法验证开发者」怎么办（未签名的种子版本）

在正式签名版发布之前，你拿到的是未经 Apple 公证的种子版本。第一次双击时 macOS 会弹出类似 **「无法打开，因为 Apple 无法检查其是否包含恶意软件」** 或 **「无法验证开发者」** 的提示，这是系统对所有未公证软件的统一提醒，不代表软件有问题。处理方法：

1. 在提示框里点「完成」（或「好」），**不要**点「移到废纸篓」。
2. 打开 系统设置 → 隐私与安全性，向下滚动到「安全性」一栏。
3. 你会看到一行「已阻止使用 "LocalPIISanitizer"…」，点右边的 **「仍要打开」**。
4. 再次确认 → 输入本机开机密码或用触控 ID → 应用启动。

「仍要打开」这一行只在你刚被拦住之后的一段时间内出现；如果在「安全性」一栏里没看到，回到「应用程序」再双击一次应用，然后马上回来看。每个新版本只需这样做一次，之后双击即可直接打开。正式签名版发布后不再有这一步。单页版说明见随包附带的《如何打开未签名版本》。

熟悉终端的用户也可以用一条命令代替上面四步（效果相同，去掉系统给下载文件打的隔离标记）：

```bash
xattr -dr com.apple.quarantine /Applications/LocalPIISanitizer.app
```

## 4. 首次准备（一次性，下载本地大模型）

识别人名和公司名需要一个在本机运行的大模型（约 3.4 GB）。第一次启动时应用会打开「首次准备」页面：

1. 点「开始下载」，页面上有进度条；下载期间可以最小化窗口，中途关闭应用下次会**断点续传**。
2. 下载完成后页面自动消失，脱敏与还原按钮变为可用。完成前这两个按钮是灰色的。
3. 下载全程只是把模型文件保存到本机，**不会上传你的任何文件**。

**网络不畅（下载很慢或反复失败）时的替代办法**：在另一台网络通畅的 Mac 上安装同一版本并完成首次准备，然后把那台电脑数据目录下的 `models` 文件夹整个复制到本机的数据目录（数据目录位置见第 6 节；「首次准备」页面的「网络不畅怎么办」里也会直接显示这个路径），再回到页面点「重试」，应用会直接识别到已有模型。

## 5. 从旧版（4.0.x，脚本安装的版本）导入数据

如果你之前用过 4.0.x（解压 zip 后跑安装脚本的版本），历史批次的加密映射表、日志与设置可以一键导入新版本，这样旧批次仍可还原：

1. 在「首次准备」页面展开 **「从旧版目录导入…」**。
2. 把旧版的安装目录路径（就是当年解压出来、里面有 `vault` 文件夹的那个目录）粘贴到输入框，点「导入」。
3. 导入**只复制、不删除**旧文件；新目录里已有的同名文件不会被覆盖。页面会显示复制了多少个文件、导入前后映射表数量。

导入完成并确认新版本一切正常后，旧版目录可以自行删除。旧版的 Homebrew 组件（`ollama`、`pandoc`、`pango`、`python@3.12`）新版本不再使用，可按需自行卸载。

## 6. 你的数据在哪里

| 内容 | 位置 |
|---|---|
| 脱敏后的文件（发给云端 AI 的版本） | `文稿/本地脱敏/脱敏文件/`（可在界面顶部改） |
| 还原后的 Word / PDF | `文稿/本地脱敏/还原文件/`（可在界面顶部改） |
| 加密映射表与密钥 `vault/`、日志 `logs/`、授权凭证 `license/`、本地模型 `models/`、原始文件与云端返回的暂存 | 应用支持目录：`~/资源库/Application Support/LocalPIISanitizer/`（访达里按 Shift-Cmd-G 粘贴 `~/Library/Application Support/LocalPIISanitizer` 可直达） |

**`vault` 文件夹绝不要同步到网盘、发给别人或删除**：里面是能把占位符换回原文的唯一钥匙，丢了就无法还原已脱敏的文件（这是有意的安全设计，我们也无法帮你找回）。建议把整个应用支持目录纳入本机的 Time Machine 备份。

应用本身（应用程序文件夹里的那个图标）只读不写，所有运行产生的内容都在上面两个位置。

## 7. 启动时提示「端口 8814 已被占用」

应用在本机的 8814 端口提供界面。如果电脑上有别的程序正占用这个端口，应用会明确报错并退出，**不会**偷偷换一个端口。处理：

- 最常见的原因是上一次的应用没有完全退出，或旧版 4.0.x 的服务还在后台跑：先退出旧版（旧版终端里 Ctrl-C，或重启电脑），再打开新版。
- 确实需要换端口时，用终端启动并指定端口，例如：
  `/Applications/LocalPIISanitizer.app/Contents/MacOS/LocalPIISanitizer --port 8824`
  （若同机装有免费版，它固定用 8813，两者互不冲突，可以同时运行。）

## 8. 激活与续费

- 企业版可免费试用 30 次（脱敏 / 还原共用一个额度）。用完后页面**顶栏出现激活码输入框**，粘贴激活码点「激活」即可（此时需要联网，超时 8 秒）。
- 购买：打开 <https://lic.socialmore.cn/buy>，微信扫码付款后**页面直接显示激活码**（请立即保存）；丢失可到 <https://lic.socialmore.cn/recover> 凭微信支付订单号找回。
- 一个激活码绑定一台电脑；换电脑请联系客服换绑，之后在新电脑用原激活码重新激活。
- 若 `lic.socialmore.cn` 打不开，可改用 <https://lic.socialmore.net>（同一后台）：在数据目录（第 6 节）新建或编辑 `config.local.yaml`，写入一行 `license: {server_url: "https://lic.socialmore.net"}` 后重启应用。
- 激活后每次启动会静默校验一次（续费 / 换绑后自动生效）；校验失败不影响使用，沿用本机凭证，断网也照常可用。

## 9. 卸载

1. 退出应用（Cmd-Q）。
2. 把「应用程序」里的 **LocalPIISanitizer** 拖到废纸篓。
3. 数据目录 `~/Library/Application Support/LocalPIISanitizer/` 与 `文稿/本地脱敏/` **不会**被自动删除。如果以后还可能需要还原旧的脱敏文件，请保留它们（至少保留其中的 `vault`）；确认不再需要时再手动删除（其中 `models` 约 3.4 GB）。

## 10. 常见问题

**双击没反应 / 图标跳两下就消失了**
　先按第 3 节完成「仍要打开」；仍不行请确认系统是 macOS 14 及以上、安装包芯片类型与本机一致（在 Intel 机器上打开 arm64 包会直接闪退）。

**界面右上角「健康」有红项**
　点开看是哪一项：
　- 「ollama」红：本地模型服务还没起来，等 10 秒刷新；若一直红，退出应用后重新打开。
　- 「模型」红：首次准备没有完成，回到首次准备页面继续下载。
　- 其它项红：属于安装包问题，请把健康页截图发给我们。

**PDF 还原失败，Word 正常**
　PDF 由应用内置的排版引擎生成，中文需要系统里至少有一套中文字体（macOS 自带的苹方即可）。若提示字体或渲染错误，先试着还原为 Word 保证不耽误工作，再把错误截图发给我们。

**退出后本地模型还在运行吗？**
　不会。用 Cmd-Q 或关闭窗口的红叉退出应用时，应用会一并停止它拉起的本地模型服务；活动监视器里不会残留 `ollama` 进程。应用使用的是自带的私有模型服务（端口 11555），不会碰你自己另外安装的 Ollama。

**脱敏结果里还是漏了某个名字**
　本地小模型识别并非 100%，界面提供的「审计」会列出疑似残留供人工复核；也可以在设置里把固定要脱敏的词加入词典。任何脱敏产物在发给云端 AI 前请自己再过一眼。

**想在浏览器里而不是应用窗口里用**
　应用运行时，用任意浏览器打开 <http://127.0.0.1:8814> 就是同一个界面（应用窗口不要关，关了服务就停了）。

---

隐私承诺：本软件运行期不上传任何文档内容；唯一的联网请求是激活 / 续费校验，请求体只有 激活码、设备指纹哈希、软件版本 三个字段。完整条款见随包的《最终用户许可协议》（LICENSE.txt），第三方组件声明见 THIRD_PARTY_LICENSES.md。
